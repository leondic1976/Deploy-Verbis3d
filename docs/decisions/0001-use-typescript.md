# ADR 0001: Use strict TypeScript

Status: accepted

TypeScript with strict, unchecked-index and exact-optional checks makes public engine contracts and
resource ownership reviewable. Runtime validation remains mandatory for network/JSON inputs.
